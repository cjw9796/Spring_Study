<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration 	PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <typeAliases>
        <!-- 타입 별칭 설정 (optional) -->
    </typeAliases>
    
     <mappers>
        <!-- 매퍼 파일 위치 설정 -->
        <mapper resource="com/mybatis/mappers/manager.xml" />
    </mappers>
    
</configuration>